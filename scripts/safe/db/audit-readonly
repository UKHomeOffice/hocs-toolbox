#!/usr/bin/env bash

export PGPASSWORD=${HOCS_AUDIT_READONLY_PASSWORD}
export PGHOST=${HOCS_AUDIT_DB_HOSTNAME}
export PGUSER=${HOCS_AUDIT_READONLY_USER_NAME}
export PGDATABASE=${HOCS_AUDIT_DB_NAME}

psql

tail -f /dev/null
