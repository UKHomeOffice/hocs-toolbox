#!/usr/bin/env bash

export PGPASSWORD=${HOCS_WORKFLOW_READONLY_PASSWORD}
export PGHOST=${HOCS_WORKFLOW_DB_HOSTNAME}
export PGUSER=${HOCS_WORKFLOW_READONLY_USER_NAME}
export PGDATABASE=${HOCS_WORKFLOW_DB_NAME}

psql

tail -f /dev/null
